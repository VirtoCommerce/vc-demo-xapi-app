import { PropertyValuePipe } from './property-value.pipe';

describe('PropertyValuePipe', () => {
  it('create an instance', () => {
    const pipe = new PropertyValuePipe();
    expect(pipe).toBeTruthy();
  });
});
