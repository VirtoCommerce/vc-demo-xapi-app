import { EnvironmentVariables } from './environment-variables.payload';

describe('EnvironmentVariables', () => {
  it('should create an instance', () => {
    expect(new EnvironmentVariables()).toBeTruthy();
  });
});
