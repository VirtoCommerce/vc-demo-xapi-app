query getOrganization($id: String!) {
  organization(id: $id) {
    id
    name
    dynamicProperties {
      name
      value
      dynamicProperty {
        dictionaryItems {
          items {
            id
            name
          }
        }
      }
    }
  }
}
