query getCurrentCustomer {
  me {
    id
    userName
    contact {
      id
      firstName
      lastName
      fullName
      organizations {
        id
        name
        addresses {
          firstName
          lastName
          line1
          phone
          email,
          countryCode,
          postalCode,
          regionName,
        }
      }
    }
  }
}
